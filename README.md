# 1. 프로젝트 개요

### 1-1. 프로젝트 명
내 곁의 도서관: 경기 (Gyeonggi Library Finder)

### 1-2. 프로젝트 목적
경기도 내 수많은 공공도서관 정보를 '경기도 공공도서관 현황' API를 통해 실시간으로 가져와 사용자에게 제공합니다.

단순한 정보 조회를 넘어, 지도 연동을 통해 시각적으로 위치를 제공하고, 회원별 '관심 도서관' 관리 및 '한 줄 팁' 공유 기능을 제공하는 커뮤니티형 정보 사이트를 구축합니다.

Spring Boot, JPA, Spring Security 등 백엔드 필수 기술 스택을 TDD(테스트 주도 개발) 방식으로 적용하여 안정적이고 확장 가능한 웹 애플리케이션을 개발합니다.

### 1-3. 대상 사용자
* 경기도민: 거주지, 직장, 학교 근처의 도서관 정보를 찾고 싶은 사용자
* 학생 및 취준생: 도서관의 열람실 정보, 운영 시간, 휴관일 등이 궁금한 사용자
* 부모: 아이와 함께 방문할 도서관의 프로그램을 찾는 사용자

---

# 2. 주요 기능 (Functional Requirements)

### 2-1. 👨‍👩‍👧‍👦 공통 기능 (비회원/회원)
* **(R) 도서관 목록 조회:** '경기도 공공도서관 현황 API'를 연동하여 도서관 목록을 조회합니다.
    * 기술: `Spring Web`, `Thymeleaf`, `Paging`
* **(R) 도서관 상세 조회:** 특정 도서관의 기본 정보(주소, 운영시간, 전화번호, 휴관일 등)를 조회합니다.
* **(R) 지도 연동 (핵심):** API로 받은 도서관의 위/경도 좌표를 이용해 카카오/네이버 지도에 마커를 표시합니다.
    * 기술: `Kakao Map API`, `Thymeleaf`, `JavaScript(fetch)`
* **(R) '한 줄 팁' 목록 조회:** 해당 도서관에 대해 다른 회원들이 남긴 팁을 조회합니다.

### 2-2. 👤 회원 전용 기능 (로그인 필요)
* **(C/R/U/D) 회원 관리:**
    * Spring Security를 이용한 회원가입, 로그인, 로그아웃 기능을 구현합니다.
    * 비밀번호는 `PasswordEncoder` (BCrypt)를 사용해 안전하게 암호화하여 저장합니다.
    * 기술: `Spring Security`, `JPA`, `Validation`
* **(C/R/D) 관심 도서관 관리 (찜하기):**
    * (Create) 마음에 드는 도서관을 '관심 도서관'으로 등록합니다.
    * (Read) 마이페이지에서 내가 찜한 도서관 목록을 조회합니다.
    * (Delete) 찜 목록에서 삭제합니다.
    * 기술: `JPA (M:N 관계)`, `Spring Security (인증)`, `AJAX`
* **(C/R/U/D) '한 줄 팁' 관리 (커뮤니티):**
    * (Create) 도서관 상세 페이지에서 방문 후기나 팁(예: "3층 열람실 창가 자리 좋아요")을 작성합니다.
    * (Read) 마이페이지에서 내가 작성한 팁 목록을 조회합니다.
    * (Update/Delete) 본인이 작성한 팁만 수정/삭제할 수 있도록 권한을 제어합니다.
    * 기술: `JPA (1:N 관계)`, `Spring Security (인가)`, `Validation`

---

# 3. 기술 스택 및 개발 환경

| 구분 | 기술 | 버전 및 사유 |
| :--- | :--- | :--- |
| **Backend** | Spring Boot | 3.5.7 (비동기 처리, 최신 기능 활용) |
| | Java | 21 (LTS 버전, 가상 스레드 등) |
| **Database** | MariaDB | 11.8.3 (운영 DB, `utf8mb4`로 이모지 지원) |
| | H2 Database | (TDD 및 테스트 환경용 In-memory DB) |
| **Data Access**| Spring Data JPA | (엔티티 중심 개발, `Repository`로 생산성 향상) |
| **Security** | Spring Security | (필터 기반의 강력한 인증/인가 처리) |
| **View** | Thymeleaf | (JSP를 대체하는 서버 사이드 렌더링 엔진) |
| **Build Tool** | Gradle - Groovy | (유연한 의존성 관리) |
| **Dev Tools** | Lombok | (코드 다이어트) |
| | Spring DevTools | (신속한 재시작으로 개발 효율 향상) |
| **API** | `data.go.kr` | (경기도 공공도서관 현황) |
| | `Kakao/Naver Map API`| (공공 데이터 및 지도 시각화) |
| **IDE** | IntelliJ Pro | (강력한 코드 어시스트 및 기능) |

---

# 4. 시스템 아키텍처 (MVC)
* 요청(Request) -> `Controller` -> `Service` (비즈니스 로직) -> `Repository` (JPA) -> DB
* AOP, Validation, Security 등 공통 관심사는 `config` 또는 `aspect` 패키지에서 관리합니다.

---

# 5. DB 설계 (E-R Diagram)
* `Member` (회원)
* `LibraryBookmark` (관심 도서관 찜)
* `LibraryTip` (한 줄 팁)

**[관계]**
* `Member (1)` : `LibraryBookmark (N)`
* `Member (1)` : `LibraryTip (N)`

*참고: 도서관 정보 자체는 API로 조회하므로 우리 DB에 저장할 필요 없이, `LibraryBookmark`나 `LibraryTip`에 API의 고유 ID(예: `library_code`)만 컬럼으로 저장하면 됩니다.*

---

# 6. 개발 계획 (TDD)
* **1차: 환경 설정 및 회원 도메인(TDD)**
    * `Member` 엔티티 및 `MemberRepository` TDD로 검증
    * `SecurityConfig` (PasswordEncoder) 설정
    * `MemberService` (회원가입 로직) TDD로 구현
* **2차: 회원 기능 및 API 연동 (TDD)**
    * 로그인/로그아웃, 회원가입 `Controller` 및 `Thymeleaf` 뷰 구현
    * `data.go.kr` API 연동 서비스 구현 (TDD - `RestTemplate` 또는 `WebClient` Mocking)
* **3차: 핵심 기능 (CRUD)**
    * '관심 도서관(Bookmark)' 도메인 설계 및 TDD
    * '한 줄 팁(Tip)' 도메인 설계 및 TDD
    * AJAX를 이용한 비동기 찜하기/팁 등록 기능 구현
* **4차: 지도 연동 및 배포**
    * `Kakao Map API` 연동 및 마커 표시
    * 최종 테스트 및 리팩토링

---

# 7. 프로젝트 구조 (Project Structure)
```text
local-info/
├── build.gradle
├── .gitignore
└── src/
    ├── main/
    │   ├── java/
    │   │   └── com/pgc/localinfo/
    │   │       ├── LocalInfoApplication.java (메인 실행 클래스)
    │   │       ├── config/         (Spring Security, WebConfig 등 설정 파일)
    │   │       ├── controller/     (HTTP 요청/응답을 처리하는 컨트롤러)
    │   │       ├── domain/         (JPA 엔티티, Enum)
    │   │       ├── dto/            (계층 간 데이터 전송 객체)
    │   │       ├── repository/     (Spring Data JPA 리포지토리 인터페이스)
    │   │       └── service/        (핵심 비즈니스 로직)
    │   └── resources/
    │       ├── application.properties (운영/개발 DB, API 키 등 설정)
    │       ├── static/         (CSS, JS, 이미지 등 정적 파일)
    │       └── templates/      (Thymeleaf HTML 템플릿 파일)
    └── test/
        ├── java/
        │   └── com/pgc/localinfo/
        │       ├── repository/
        │       │   └── MemberRepositoryTest.java (리포지토리 TDD)
        │       └── service/
        │           └── MemberServiceTest.java (서비스 TDD)
        └── resources/
            └── application-test.properties (H2 DB 등 테스트 전용 설정)